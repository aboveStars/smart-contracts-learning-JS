## To test on a test-net:

-   Get `subID` for `chainLink VRF`
-   **Deploying** contracts with `subID`
-   **Register** the contract with `ChainlinkVRF & SUB_ID`
-   **Register** contract with `Chainlink Keepers`
-   **Run tests**
